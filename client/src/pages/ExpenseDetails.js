import React from 'react';
import { Container, Typography, Box } from '@mui/material';

// Create individual page components
export const ExpenseDetails = () => (
  <Container maxWidth="lg">
    <Box sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>Expense Details</Typography>
      <Typography variant="body1">Expense details page - Coming soon</Typography>
    </Box>
  </Container>
);

export const CreateExpense = () => (
  <Container maxWidth="lg">
    <Box sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>Create Expense</Typography>
      <Typography variant="body1">Create expense page - Coming soon</Typography>
    </Box>
  </Container>
);

export const Approvals = () => (
  <Container maxWidth="lg">
    <Box sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>Approvals</Typography>
      <Typography variant="body1">Approvals page - Coming soon</Typography>
    </Box>
  </Container>
);

export const Analytics = () => (
  <Container maxWidth="lg">
    <Box sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>Analytics</Typography>
      <Typography variant="body1">Analytics page - Coming soon</Typography>
    </Box>
  </Container>
);

export const Settings = () => (
  <Container maxWidth="lg">
    <Box sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>Settings</Typography>
      <Typography variant="body1">Settings page - Coming soon</Typography>
    </Box>
  </Container>
);

export const Profile = () => (
  <Container maxWidth="lg">
    <Box sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>Profile</Typography>
      <Typography variant="body1">Profile page - Coming soon</Typography>
    </Box>
  </Container>
);

export const CompanySettings = () => (
  <Container maxWidth="lg">
    <Box sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>Company Settings</Typography>
      <Typography variant="body1">Company settings page - Coming soon</Typography>
    </Box>
  </Container>
);

export const UserManagement = () => (
  <Container maxWidth="lg">
    <Box sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>User Management</Typography>
      <Typography variant="body1">User management page - Coming soon</Typography>
    </Box>
  </Container>
);

export const Reports = () => (
  <Container maxWidth="lg">
    <Box sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>Reports</Typography>
      <Typography variant="body1">Reports page - Coming soon</Typography>
    </Box>
  </Container>
);

export const AuditLog = () => (
  <Container maxWidth="lg">
    <Box sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>Audit Log</Typography>
      <Typography variant="body1">Audit log page - Coming soon</Typography>
    </Box>
  </Container>
);

// Create individual files
export default ExpenseDetails;